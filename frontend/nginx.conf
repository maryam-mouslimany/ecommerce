server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router SPA fallback
    location / {
        try_files $uri /index.html;
    }

    # Optional: Handle static file caching
    location /static/ {
        expires 1y;
        add_header Cache-Control "public";
    }
}
